# Vim的移动

- 标记当前位置，在移动到其他位置处理完问题后，再返回到前一个标记位置。

`'{mark}`：移动到标示设置所在行，将光标放置在第一个非空字符。
`\`{mark}`：移动到光标设置的位置，包括所在列。



- 将光标所在行移动到顶部、中间、尾部

`z<Enter>`：将光标所在行移动文件的顶部；

`z.`：将光标行移到文件的中间位置；

`z-`：将光标所在行的位置移动到文件尾部。




