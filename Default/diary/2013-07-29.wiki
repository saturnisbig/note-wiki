

## 数据库的命名原则

1. 表名只能由大小写英文字母组成，不用下划线，不添加任何表前缀
2. 每个字段或索引独占一行，以便自动新增字段程序对table\_template进行分析
3. 所有表的主键的都必须是"表名"+"id"，比如Userid，Bookid,UserInfoid等
4. 如果某字段存储的数据为另一个表的主键值，则此字段必须与另一表的主键名相同，
比如Comment表需要保存书籍的id，那么这个字段必须为Bookid。
5. 其他普通字段均使用小写单词加下划线分隔，比如login\_ip。


